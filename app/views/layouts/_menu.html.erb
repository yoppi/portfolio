<div id="menu">
<ul>
<% @menus.each do |menu| %>
  <li>
  <%=
    link_to_unless(current_controller?(menu), menu, {:controller => menu}) do
      link_to(menu, {:controller => menu}, {:class => 'on'})
    end
  %>
  </li>
<% end %>
</ul>
</div>
